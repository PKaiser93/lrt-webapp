# MongoDB-Verbindungs-URL (z.B. für Docker Compose)
MONGO_URI=mongodb://BENUTZER:PASSWORT@mongo:27017/DATENBANKNAME

# Server-Port (Standard: 3000)
PORT=3000

# Erlaubte Ursprünge (CORS, Komma-getrennt, z.B. lokale und produktive Frontends)
CORS_ORIGINS=http://localhost:8080,http://localhost:5173

# JWT Secret für Authentifizierung (hier nur ein Platzhalter!)
JWT_SECRET=dein_jwt_secret_hier_eintragen
